<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="oval">
            <size android:width="13dp" android:height="13dp" />
            <solid android:color="#FF0000" /> <!-- Color del círculo cuando está seleccionado -->
            <stroke android:width="2dp" android:color="#000000" /> <!-- Borde del círculo -->
        </shape>
    </item>
    <item android:state_checked="false">
        <shape android:shape="oval">
            <size android:width="13dp" android:height="13dp" />
            <solid android:color="#FFFFFF" /> <!-- Color del círculo cuando no está seleccionado -->
            <stroke android:width="2dp" android:color="#000000" /> <!-- Borde del círculo -->
        </shape>
    </item>
</selector>